import 'package:flutter/material.dart';

class CookingStepsScreen extends StatelessWidget {
  const CookingStepsScreen({super.key});

  static const String id = '/cooking-steps';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
